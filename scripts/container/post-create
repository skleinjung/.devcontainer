#!/usr/bin/env bash
set -euo pipefail

LOG_DIR="/workspace/.devcontainer/logs"
mkdir -p "$LOG_DIR"
exec > >(tee "$LOG_DIR/container.log") 2>&1

echo "[post-create] Started."

# Drop-ins run best-effort: a failing one is logged and skipped so it can't take
# down the rest of the create.
status=0
for script in /usr/local/bin/post-create.d/*.sh; do
  [ -f "$script" ] || continue
  echo "[post-create] Running $(basename "$script")..." >&2
  rc=0
  bash "$script" || rc=$?
  if [ "$rc" -ne 0 ]; then
    echo "[post-create] $(basename "$script") failed (exit $rc); continuing." >&2
    status=1
  fi
done

echo "[post-create] Complete."
exit "$status"
